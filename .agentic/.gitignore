# dogfood — ephemerální projekty pro testování flow, mimo verzování
dogfood/

# Python bytecode cache (core/ moduly)
__pycache__/
*.pyc

# Claude Code — sdílený allowlist (settings.json) se verzuje; lokální/osobní ne
.claude/settings.local.json
